import styled from 'styled-components';

export const AppContainer = styled.footer`
    min-height: 100vh;
    position: relative;
    padding-bottom: 60px;
    overflow-x: hidden; // Dodaj tę właściwość
`;